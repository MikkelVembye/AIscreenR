# Test if the object is a `'chatgpt'` object

**\[deprecated\]**  
  
This function returns `TRUE` for `chatgpt` objects, and `FALSE` for all
other objects.

## Usage

``` r
is_chatgpt(x)
```

## Arguments

- x:

  An object

## Value

`TRUE` if the object inherits from the `chatgpt` class.
