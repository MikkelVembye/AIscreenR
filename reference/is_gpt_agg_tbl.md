# Test if the object is a `'gpt_agg_tbl'` object

This function returns `TRUE` for `gpt_agg_tbl` objects, and `FALSE` for
all other objects.

## Usage

``` r
is_gpt_agg_tbl(x)
```

## Arguments

- x:

  An object

## Value

`TRUE` if the object inherits from the `gpt_agg_tbl` class.
