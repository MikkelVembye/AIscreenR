# Test if the object is a `'gpt'` object

This function returns `TRUE` for `gpt` objects, and `FALSE` for all
other objects.

## Usage

``` r
is_gpt(x)
```

## Arguments

- x:

  An object

## Value

`TRUE` if the object inherits from the `gpt` class.
